import React, {Component} from 'react'

class TextInput extends component {
    //Render is called when the component has been rendered or rerendered.
    render(){
        return (
            <div> Hello </div>      )
    }
}

//make it available to the rest of the app
export default TextInput